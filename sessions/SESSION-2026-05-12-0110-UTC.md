--- SESSION SNAPSHOT ---
TIMESTAMP: 2026-05-12 01:10 UTC
Captured by: Claude Code
Posture at capture: FIX (implicit -- diagnosing broken UI)
Trigger: session close / handoff report to claude.ai

WHAT THIS SESSION WAS DOING
Executed the NEXT IMMEDIATE ACTION from SESSION-2026-05-12-0045-UTC: git pull,
dev server start, open /investigation, smoke test Module 16. Discovered the UI
rendering as unstyled text (broken). Diagnosed as a stale .next build cache from
a dev server that had been running for 26 hours predating all recent changes.
Killed the old server, cleared the cache, cold-started clean, verified all routes
200 and CSS assets resolving. Module 16 functional smoke test (create investigation
+ send message) still requires a human in the browser.

STATE AT CAPTURE -- TIMESTAMP: 2026-05-12 01:10 UTC
Dev server running clean at localhost:3000 (fresh cold start after cache clear).
git pull confirmed. All 15 ENGINE_FILES paths verified against real files on disk.
npx tsc --noEmit exit 0, no type errors.
All routes 200: /, /investigation, /timeline, /api/persons, /api/timeline.
Working tree has uncommitted tsconfig.json auto-edit (Next.js added target:ES2017
and reformatted arrays on first cold start). Left uncommitted -- user to decide.
Also untracked: .claude/ (worktree state) and package-lock.json (not committed in this project).

DECISIONS MADE THIS SESSION

TIMESTAMP: 2026-05-12 01:00 UTC -- Diagnosed CSS 404 as stale .next cache, not a
  code regression. Confirmed by curling asset URLs directly:
  GET /_next/static/css/app/layout.css -> 404 on the 26h-old server.
  GET / -> 404 also. JS chunks -> 200 (hydration fine; not a JS problem).
  The asset hash in the HTML no longer matched any file on disk.

TIMESTAMP: 2026-05-12 01:05 UTC -- Fix: kill old next-server (PID 39557),
  rm -rf .next, npm run dev. All routes green after cold start.

REJECTED:
- Do not assume a broken UI is a code regression. Curl the CSS asset URL first.
- Do not start a new dev server without checking lsof -iTCP:3000 first -- a
  silently occupied port will land the new server on 3001, not 3000.

OPEN THREADS -- TIMESTAMP: 2026-05-12 01:10 UTC

1. Module 16 functional smoke test: create one investigation, send one message,
   confirm AI responds with full investigation context loaded. Requires human in browser.
   Dev server is clean and ready at localhost:3000/investigation.

2. tsconfig.json auto-edit in working tree. Standard Next.js cold-start behavior.
   Not a bug. User to decide whether to commit.

3. All prior open threads from SESSION-2026-05-12-0045-UTC carry forward:
   voice profile discussion, platform name, Steve Little collaboration,
   investigation types in src/types/index.ts, gps_stage_reached constraint.

PARTIALLY BUILT WORK
None. Git is clean (aside from uncommitted tsconfig.json auto-edit).

DO NOT DO THIS
- Do not treat a broken UI as a code regression before curling the asset URLs.
- Do not start a new dev server if port 3000 is already occupied -- check first
  with lsof -iTCP:3000, kill the old process, then restart.
- After any session adds routes/CSS/prompts, restart the dev server cleanly
  before the next browser smoke test. A long-running server will serve stale assets.
- Do not use the Greene/Greenspun line as test data.

NEXT IMMEDIATE ACTION
TIMESTAMP: 2026-05-12 01:10 UTC
Open localhost:3000/investigation in the browser.
Create a test investigation, send one message, verify the AI responds with
the investigation's problem statement and context loaded in its reply.
--- END SNAPSHOT ---
