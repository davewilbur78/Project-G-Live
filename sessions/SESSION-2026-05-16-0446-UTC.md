--- SESSION SNAPSHOT ---
TIMESTAMP: 2026-05-16 04:46 UTC
Captured by: Claude Code (claude-sonnet-4-6)
Posture at capture: BUILD
Trigger: session close (PersonExternal initiative complete pending one optional cleanup)

WHAT THIS SESSION WAS DOING
Final phase of the PersonExternal initiative. claude.ai ran cleanup SQL in
Supabase (DELETE FROM persons WHERE ancestry_id LIKE 'ftm:%') to wipe FTM
person duplicates that the prior persons-dedup bug had created. This session:
applied the pagination fix to import-ftm.mjs:405-407 (chunked .range() loop
to bypass PostgREST's 1000-row select cap), committed it, ran the importer
twice against the synced .ftm with fresh extracts, and verified the three
target counts hit exactly and were idempotent across both runs.

STATE AT CAPTURE -- TIMESTAMP: 2026-05-16 04:46 UTC

Worktree branch claude/vigorous-lamport-a860aa, commits ahead of main:
  97d1f36  feat: add PersonExternal + Sync_Person extraction to ftm-extractor.c
  260fe5f  session: PersonExternal schema investigation
  69fcc5a  feat: migration 019 + importer Phase 7 for person_external_ids
  5817718  session: bug exposure snapshot
  9886492  fix: paginate existing-persons fetch in import-ftm.mjs
  (this session's snapshot commit will be next)

Live in Supabase (verified with count:'exact' queries):
  persons total:                 1,577  (1,576 FTM + 1 pre-existing)  ✓
  persons ftm:* (FTM-sourced):   1,576                                ✓
  person_external_ids total:     1,576                                ✓
    provider=ancestry:           1,576                                ✓
    provider=familysearch:       0                                    ✓
  timeline_events:               5,983                                ✓
  family_members:                2,204
  families:                      1,713  -- ANOMALY, see Open Threads

Idempotency confirmed: all three target counts unchanged between run 1
(04:31 UTC) and run 2 (04:36 UTC). Run 2 logged "0 persons inserted, 1576
updated" -- the smoking gun that the pagination fix works (was
"576 inserted, 1000 updated" before the fix).

DECISIONS MADE THIS SESSION
TIMESTAMP: 2026-05-16 04:30 UTC -- Pagination via chunked .range() loop in
  the existing-persons fetch. Picked over server-side RPC (no schema change)
  and over adding a unique constraint on persons.ancestry_id (smaller blast
  radius). Self-contained 14-line change.
TIMESTAMP: 2026-05-16 04:46 UTC -- Do not auto-clean the 1,088 orphan
  families. claude.ai owns that cleanup decision. Snapshot recommends the
  one-line SQL.

OPEN THREADS -- TIMESTAMP: 2026-05-16 04:46 UTC

1. ORPHAN FAMILIES (collateral debris from prior buggy runs):
   1,088 families have both partner1_id AND partner2_id = NULL.
   Root cause: when the cleanup SQL deleted FTM persons, the
   families.partner_id FKs appear to be ON DELETE SET NULL (not CASCADE),
   leaving these families behind as orphans. Today's fresh import inserted
   625 new families on top of the 1,088 orphans + 96 pre-existing = 1,713.

   Recommended cleanup (claude.ai via Claude in Chrome):
     DELETE FROM families WHERE partner1_id IS NULL AND partner2_id IS NULL;

   Do NOT delete the 89 single-NULL families -- those could be legit FTM
   single-parent records.

2. PERSONS UNIQUE CONSTRAINT (architectural improvement, optional):
   persons.ancestry_id has no unique constraint, hence the manual
   select+update/insert dance in the importer. Adding
     CREATE UNIQUE INDEX ON persons(ancestry_id) WHERE ancestry_id IS NOT NULL;
   would let the importer use a real .upsert() and would prevent any future
   recurrence of duplicate-persons accumulation. Not urgent now that the
   pagination fix lands, but worth considering.

3. WORKTREE MERGE: branch claude/vigorous-lamport-a860aa has 5 unmerged
   commits. Dave's call on merge timing/strategy.

PARTIALLY BUILT WORK
None.

DO NOT DO THIS
- Do NOT delete the 89 single-NULL families without inspection -- they may
  be legitimate FTM single-parent relationships.
- Do NOT use --skip-extract after recompiling.
- Do NOT use git add -A.
- Do NOT assume the pagination fix is sufficient on a 5,000-person tree --
  the cap math still holds (PAGE=1000 plus loop), but please verify before
  declaring victory on much larger trees.

NEXT IMMEDIATE ACTION
TIMESTAMP: 2026-05-16 04:46 UTC
Hand off to claude.ai for the optional orphan-families cleanup SQL via
Claude in Chrome. The PersonExternal initiative itself is complete:
  - Migration 019 live
  - person_external_ids = 1,576 (all ancestry, idempotent)
  - Pagination bug fixed and verified

--- END SNAPSHOT ---
