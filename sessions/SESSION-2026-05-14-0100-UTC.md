--- SESSION SNAPSHOT ---
TIMESTAMP: 2026-05-14 01:00 UTC
Captured by: Claude Code (claude-sonnet-4-6)
Posture at capture: BUILD
Trigger: phase completion / session close

WHAT THIS SESSION WAS DOING
FTM Bridge Phase 2. Picked up from the Phase 1 foundation (143 persons live in Supabase).
This session implemented full idempotency on families and timeline_events, wired
SourceLink citations to timeline_events (the GPS evidence chain), imported alternate names
from NAME/ALIA facts with GEDCOM slash cleaning, and audited the UI for raw FTM codes.

The living flag (IsLiving) was investigated but the column simply does not exist in FTM
2024's schema version 20200615. The Person table has no IsLiving field -- FTM computes
living status at runtime. The field remains false for all imported persons.

KEY FINDINGS

SourceLink structure confirmed:
  data.sourceLinks[].LinkTableID = 2 for ALL sourceLinks in this file.
  Table ID 2 = FTM's Fact table (confirmed by intersection with data.facts).
  3606 total sourceLinks → 1454 unique fact→source links built.
  1117 of 1189 timeline events (93.8%) now have source_id populated in Supabase.
  The source_id wiring was the most important Phase 2 deliverable and is now live.

Idempotency strategy: delete-then-reinsert.
  After personIdMap and sourceIdMap are built, Phase 3.5 deletes all FTM-sourced
  families and timeline_events. Then Phases 4-6 re-insert everything fresh.
  This is correct for a sync tool where FTM is authoritative.
  Persons are NOT deleted -- they use upsert and may have manually-added research data.

FTM 2024 Person table schema: confirmed 48 columns, no IsLiving.
  Full column list documented in this session's debugging output.
  Living flag remains false (the actual FTM value is a runtime computation
  based on death date and birth year, not a stored column).

Place name parsing: parseFTMPlace() confirmed correct.
  Both FTM place formats handled: pipe-delimited (/Display Name|//) and
  slash-hierarchy (///County/State/Country/numericCode/lat/lon).
  0 pipe or slash characters in any stored place_name after parsing.

Alternate names: 5 persons have alt_names populated.
  GEDCOM /Surname/ delimiters stripped via cleanGedcomName().
  Duplicates removed via Set deduplication.
  Example: Stanley Samuel Kwass has ['Samuel Kwasnistsky', 'Stanley S Kwas'] correctly.

STATE AT CAPTURE -- TIMESTAMP: 2026-05-14 01:00 UTC
All committed. Repo clean.

AGENT.md: v2.9.2 (this session)

scripts/ftm-extractor.c:
  Added comment noting IsLiving column absence.
  Debug schema section (PRAGMA table_info) added and removed during investigation.
  Binary recompiled (ARM64, same compile command as before).

scripts/import-ftm.mjs:
  Phase 2 additions:
  - cleanGedcomName(): strips /Surname/ GEDCOM delimiters from alt_name text
  - altNamesMap: built from FactClass=257 NAME/ALIA facts, deduplicated per person
  - alt_names and living (false, noted) added to person mapping
  - sources: now fully idempotent (fetch+match by ee_full_citation, insert only new)
  - factSourceMap: built from sourceLinks (LinkTableID=2) → sourceCitations → masterSources
  - Phase 3.5: delete prior FTM families + timeline_events before re-insert
  - Phase 6: source_id: factSourceMap.get(f.ID) ?? null added to eventRows
  - Header comment updated to note idempotency is now automatic
  - writeFileSync added to extractFTMData() to cache JSON for --skip-extract

ACCEPTANCE TEST RESULTS -- TIMESTAMP: 2026-05-14 01:00 UTC
Test file: /Users/dave/ftm playground /Mom plus 1 generation.ftm
Run 1: 96 families, 238 family members, 1189 timeline events, 1117 with source_id wired
Run 2 (--skip-extract): 96 families, 238 family members, 1189 timeline events, 1117 with source_id
→ PASS: identical counts on re-run, zero duplicates

Supabase spot-check:
  place_name with pipe/slash: 0 ✓
  timeline_events with source_id: 1117/1189 (93.8%) ✓
  alt_names populated: 5 persons ✓
  living=true: 0 (expected -- no IsLiving column in this FTM schema) ✓

DECISIONS MADE THIS SESSION
2026-05-14 UTC -- Living flag is not importable from FTM 2024 schema 20200615.
  The Person table has 48 columns and none is IsLiving. FTM computes it at runtime
  from death date absence + birth year recency. We leave living=false for all FTM
  imports. A future enhancement could derive it heuristically (no death date +
  born after ~1920) but that is out of scope for Phase 2.

2026-05-14 UTC -- SourceLink.LinkTableID=2 confirmed as the FTM Fact table ID.
  Hard-coded in the factSourceMap building loop. All sourceLinks in this test file
  have LinkTableID=2. If the full tree produces other values, investigate.

2026-05-14 UTC -- Delete-then-reinsert is the correct idempotency strategy for
  families and timeline_events. FTM is authoritative. No need for query-diff logic.

2026-05-14 UTC -- The importer now auto-saves /tmp/ftm_data.json on each extraction
  run, making --skip-extract reliable without a separate manual step.

OPEN THREADS -- TIMESTAMP: 2026-05-14 01:00 UTC
1. Run full synchronized tree (~1500 persons): Phase 2 idempotency is done.
   The full tree file is not available in this session. Ready to run when provided.
2. Person detail page: no UI page to drill into a person's details yet.
   /persons/[id] exists but is minimal (name, birth/death, notes only).
   Needs families view, full timeline, alt_names display, source inventory.
3. Person external IDs (migration 019): deferred. Will need PersonExternal data
   from the synchronized tree.
4. Cause of death (_DCAUSE): FactClass=257, still not imported.
5. Media files: needs Supabase storage bucket.
6. Deployment to Vercel: not yet done.
7. Voice profile discussion: still pending (required before Module 9).

DO NOT DO THIS
- Do not try to import IsLiving from FTM -- the column does not exist in schema 20200615.
- Do not add migration 019 (person_external_ids) until synchronized tree is available.
- Do not run the full tree without first verifying --skip-extract works on the new JSON
  (since ftm-extractor.c was not changed substantively, the binary is compatible).
- Do not use the Greene/Greenspun line as test data.

NEXT IMMEDIATE ACTION
TIMESTAMP: 2026-05-14 01:00 UTC
Run the full synchronized tree when the file is available.
Before running: confirm SourceLink LinkTableID=2 still holds (the diagnostic log
line was removed; re-add temporarily if the full tree behaves unexpectedly).
Also consider building the person detail page before the full tree run so you
can visually inspect individual records at scale.
--- END SNAPSHOT ---
