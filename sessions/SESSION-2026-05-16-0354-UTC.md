--- SESSION SNAPSHOT ---
TIMESTAMP: 2026-05-16 03:54 UTC
Captured by: Claude Code (claude-sonnet-4-6)
Posture at capture: BUILD
Trigger: session close

WHAT THIS SESSION WAS DOING
Updated scripts/ftm-extractor.c to add PersonExternal and Sync_Person
table extractions. Before touching the C file, inspected both tables directly
in the SQLite database using the SEE encryption credentials. Confirmed schemas,
row counts, and data presence. Recompiled (clang -arch arm64) and ran a fresh
extraction against the synced .ftm file. Produced full findings report for
claude.ai to use in designing migration 019.

STATE AT CAPTURE -- TIMESTAMP: 2026-05-16 03:54 UTC
Committed: scripts/ftm-extractor.c updated (commit 97d1f36 on worktree branch
claude/vigorous-lamport-a860aa). 24 insertions -- PersonExternal and
syncPersons sections added. Compiled binary at scripts/ftm-extractor (gitignored,
in worktree only -- also deployed to /Users/dave/Project-G-Live/scripts/ as
the shared local binary). Fresh JSON at /tmp/ftm_data_new.json (40,664 lines).
Standard /tmp/ftm_data.json is stale (prior session, no syncPersons section).

DECISIONS MADE THIS SESSION
TIMESTAMP: 2026-05-16 03:30 UTC -- Add Sync_Person to extractor alongside
PersonExternal. PersonExternal is empty; Sync_Person has AmtId (Ancestry person
ID) for all 1,576 persons. Both tables added. Reason: extracting only an empty
table would return zero actionable data; Sync_Person is where the Ancestry IDs
actually live.

TIMESTAMP: 2026-05-16 03:30 UTC -- Sonnet adequate for this session. Prior
Opus session (2026-05-16 00:53 UTC) already did full-tree analytical pass.
This session is mechanical: inspect schema, update C extractor, recompile, run.
AGENT.md Opus rule applies to the deep-analysis full-tree session, which is done.

OPEN THREADS -- TIMESTAMP: 2026-05-16 03:54 UTC
- Migration 019 design: claude.ai's job. See findings below -- full schema picture
  is now available for design. Key input: Ancestry IDs live in Sync_Person.AmtId,
  not PersonExternal. FSIDs are absent from this tree entirely.
- Import pipeline update: import-ftm.mjs does not yet consume syncPersons section.
  That wiring is part of migration 019 implementation, not this session.
- The compiled binary in the worktree (scripts/ftm-extractor) should be
  propagated to /Users/dave/Project-G-Live/scripts/ftm-extractor so the main
  project has the updated binary. (Or re-run compile from main project dir after
  merge.) The C source commit is what matters for reproducibility.
- Worktree branch claude/vigorous-lamport-a860aa needs merge to main at Dave's
  direction (same pattern as prior sessions).

PARTIALLY BUILT WORK
None. C source is committed. Fresh JSON extract complete. No import-ftm.mjs
changes (per task directive). No migration 019 (per task directive).

DO NOT DO THIS
- Do NOT propose migration 019 in this session. That is claude.ai's design task.
- Do NOT use --skip-extract after recompiling ftm-extractor.c. Always fresh extract.
- Do NOT use git add -A. Stage specific paths only.
- Do NOT attempt to re-run import-ftm.mjs in this session without updating it
  to consume the new syncPersons section. The importer currently ignores any
  JSON key it doesn't recognize, so the import would run clean but would not
  write Ancestry IDs anywhere.

NEXT IMMEDIATE ACTION
TIMESTAMP: 2026-05-16 03:54 UTC
Hand findings report to claude.ai for migration 019 design. The full findings
are in the session close message. Key inputs for claude.ai:
  - Ancestry IDs: Sync_Person.AmtId, numeric strings, 1,576 of 1,576 present
  - FSIDs: absent from this tree (FamilySearchId = NULL everywhere)
  - PersonExternal: 0 rows (empty), schema documented
  - Sync_Person: 7-column table, FtmId joins to Person.ID
  - New extractor JSON section name: "syncPersons"

=== FINDINGS REPORT (for claude.ai migration 019 design) ===

PERSONEXTERNAL TABLE
  Status: EMPTY (0 rows)
  Columns (9): ID, PersonID, ProviderID (INT), ExternalID (TEXT),
    Status (TEXT), CreateDate (DATETIME), UpdateDate (DATETIME),
    SyncVersion (INT), UID (TEXT)
  Note: ProviderID is a numeric code. Table purpose is generic cross-provider
    IDs. In this Ancestry-synced tree, FTM chose not to populate it.
    May populate if the tree is also linked to FamilySearch or other providers.

SYNC_PERSON TABLE (where Ancestry IDs actually live)
  Status: POPULATED -- 1,576 rows (one per person, 100% coverage)
  Columns (7): ID, FtmId (INTEGER), AmtId (NVARCHAR(50)),
    FamilySearchId (TEXT), CreateDate (DATETIME), ModifyDate (DATETIME)
  FtmId: matches Person.ID exactly (confirmed join)
  AmtId: Ancestry Member Tree person ID. Populated for ALL 1,576 persons.
    Format: numeric string, e.g., 102343234148
    These are the IDs that construct Ancestry profile URLs.
  FamilySearchId: NULL for ALL 1,576 persons. FamilySearch link not established.

FAMILYSEARCHMATCHES TABLE
  Status: EMPTY (0 rows)
  Has FSPersonID column (TEXT) but no data in this tree.

WEBLINK TABLE
  Status: 2,209 rows. All linked to LinkTableID=16 (unknown table ID --
    likely Source or SourceLink table by FTM's internal numbering).
  Content: mostly FamilySearch ARK IDs (record/document level, not person level)
    plus Ancestry search URLs, newspapers.com clips, findagrave, etc.
  These are RECORD-level identifiers, not PERSON-level FSIDs.

ANSWER TO KEY QUESTIONS
  Ancestry IDs present? YES -- Sync_Person.AmtId, 1,576/1,576 persons
  FSIDs (person-level) present? NO -- NULL everywhere in this tree
  FamilySearch ARK IDs (record-level) present? YES -- 1,479 in WebLink

SAMPLE SYNC_PERSON ROWS
  FtmId=1  AmtId=102343234148  FSID=NULL  Name=Cynthia Chestina WILBUR
  FtmId=2  AmtId=102343234149  FSID=NULL  Name=William Oliver Wilbur
  FtmId=3  AmtId=102343234150  FSID=NULL  Name=Stephen Frances WILBUR
  FtmId=4  AmtId=102343234151  FSID=NULL  Name=Amos Elliott Wilbur
  FtmId=5  AmtId=102343234152  FSID=NULL  Name=Phoebe Agusta Wilbur

=== END FINDINGS REPORT ===

--- END SNAPSHOT ---
