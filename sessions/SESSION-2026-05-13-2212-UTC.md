--- SESSION SNAPSHOT ---
TIMESTAMP: 2026-05-13 2212 UTC
Captured by: Claude Code (claude-opus-4-7)
Posture at capture: BUILD (FTM import pipeline complete; data live in Supabase)
Trigger: end-of-session commit + documentation per claudeai PM request

WHAT THIS SESSION WAS DOING

Goal: read the FTM .ftm file directly as SQLite (no GEDCOM intermediate)
so Project-G-Live can ingest real genealogy data from Dave's Family Tree
Maker 2024 file at:
  /Users/dave/ftm playground /Mom plus 1 generation.ftm

This was a continuation of a prior context-limit session that had already:
- Identified .ftm files as SQLite SEE-encrypted (NOT SQLCipher)
- Found the SEE activation key by disassembling FTMDatabaseFoundation
- Traced the password derivation chain through FTMDataModel's ObjC + Swift code
- Decoded the XOR-encrypted schema name strings

This session's job: finalize the 39-char password, prove it opens the file,
build the import pipeline, run it, verify the data.

WHAT HAPPENED -- TIMELINE

1. Confirmed full 39-char password works on the .ftm file.
   The prior session had only tested the truncated 32-char version (which
   failed). The full version succeeded immediately and the database opened.

2. Mapped the FTM internal schema by querying sqlite_master and PRAGMA
   table_info for all 109 tables. Key tables: Person, Relationship,
   ChildRelationship, Fact, FactType, Place, MasterSource, Source,
   SourceLink, Repository, Note. Drew the LinkID + LinkTableID polymorphic
   pattern: Fact.LinkTableID=5 → Person, =7 → Relationship.

3. Reverse-engineered the FTM date encoding empirically by matching
   known dates (death of Person 42 = Jan 1 2019 per obituary URL):
     date_int = JDN * 512 + flags
   Verified on multiple dates. Flags in low 9 bits encode precision
   (year-only vs year+month vs full date) and calendar type.

4. Built scripts/ftm-extractor.c -- ARM64 C program that:
   - dlopen-loads FTMDatabaseFoundation's own SQLite/SEE engine
   - Calls sqlite3_activate_see("7bb07b8d471d642e")
   - Opens the .ftm read-only with the 39-char password
   - Dumps all genealogy tables to stdout as one big JSON document
   Compile: clang -arch arm64 -o scripts/ftm-extractor scripts/ftm-extractor.c

5. Built scripts/import-ftm.mjs -- Node.js importer that:
   - Spawns ftm-extractor with DYLD_FRAMEWORK_PATH set
   - Parses the JSON
   - Decodes FTM dates → display strings + ISO sort dates
   - Parses FTM's pipe/slash-delimited place strings
   - Strips RTF formatting from notes
   - Maps Sex 0/1 → M/F/U
   - Maps GEDCOM tags → our event_type enum
   - Upserts persons (manual upsert by ancestry_id since no unique constraint)
   - Inserts families, family_members, sources, repositories, timeline_events
   - Filters to preferred-only facts for BIRT/DEAT/BURI/BAPM to avoid duplicates

6. First live run failed at persons phase: ON CONFLICT requires a unique
   constraint on ancestry_id, which doesn't exist in our schema.
   Switched to manual upsert (SELECT + branch to INSERT or UPDATE).

7. Second live run succeeded:
     5 repositories
     143 persons inserted
     371 sources inserted
     96 families inserted
     238 family members
     1226 timeline events
   (Subsequent dedup fix reduces events to 1189 on next run.)

8. Verified data quality in Supabase: all 143 have display_name; 120/143
   have birth dates; oldest ancestor (Yakab Ripner) born 1796. Family
   memberships and marriage dates resolved correctly for spot-checked
   persons.

KEY ARTIFACTS PRODUCED

- scripts/ftm-extractor.c       -- C source for the FTM dumper
- scripts/ftm-extractor         -- compiled ARM64 binary (gitignored)
- scripts/import-ftm.mjs        -- Node.js importer
- memory: project_ftm_import.md -- keys, paths, commands documented

ENCRYPTION DETAILS (committed-to-repo summary)

  File format:    SQLite SEE (Encryption Extension), NOT SQLCipher
  Activation key: 7bb07b8d471d642e
                  (passed to sqlite3_activate_see before opening)
  DB password:    aes256:ViDfwQnOAX8IGG5T5xs3yyBOryIqfPu6
                  (39 chars; derived from schema version 20200615 via
                   XOR decode of FTMDataModel binary at offset 0x1687b0)
  SQLite source:  Must use FTMDatabaseFoundation.framework's own SQLite
                  (system sqlite3 and sqlcipher both fail)
  Framework path: DYLD_FRAMEWORK_PATH=/Applications/Family Tree Maker 2024.app/Contents/Frameworks
                  (needed because FTMDatabaseFoundation links FTMCore via @rpath)

  Older FTM schema versions use different passwords -- decoded list:
    Schema A (>=20200615): aes256:ViDfwQnOAX8IGG5T5xs3yyBOryIqfPu6
    Schema B (>=20160500): aes256:Ud1lo0OtDABLU63tRhUlLuzAJA8hNZAE
    Schema C (>=20160317): aes128:DScnaANSEN6uvDLr3HNN+0VfrPK6YODJ
    Schemas D/E/F/G older -- offsets noted in prior session summary

OPEN ITEMS / DECISIONS DEFERRED

- Media import: out of scope Phase 1. The .ftmb archive's Media/ folder
  is untouched. No media table exists in our Supabase schema yet.
- Source citation linking to timeline events: SourceLink data extracted
  but events have source_id=null. Wire-up is a future enhancement.
- Full idempotency: persons are safely re-importable; everything else
  needs manual cleanup before re-running (SQL documented in script header).
- Alternate names, cause of death, source quality ratings: extractable
  from FTM but not imported (attribute facts class 257 are skipped).
- Scale: tested on 143-person file. Larger trees (10k+ persons) would
  benefit from streaming JSON and batched person upserts.

POSTURE FOR NEXT SESSION

The FTM ingestion pipeline is working end-to-end and data is live in
Supabase. Reasonable next steps: wire source citations into timeline
events, address out-of-scope items above as needed, or move on to UI
work that uses the new data.
